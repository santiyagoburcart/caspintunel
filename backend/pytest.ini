[pytest]
# --ds has the highest precedence in pytest-django (beats the env var that
# compose sets for the container), so the suite always runs on test settings.
addopts = -q --ds=config.settings.test
python_files = tests.py test_*.py *_tests.py
